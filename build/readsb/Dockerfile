# This image is built by infra CI from the bharatradar/readsb fork (dev branch).
# This Dockerfile is a placeholder for local builds.
# For CI, the fork source is checked out directly.
FROM ghcr.io/bharatradar/readsb:latest
