# This image is built by infra CI from the bharatradar/mlat-server-sync-map fork (master branch).
# This Dockerfile is a placeholder for local builds.
# For CI, the fork source is checked out directly.
FROM ghcr.io/bharatradar/mlat-server-sync-map:latest
COPY syncmap/index.html /usr/share/nginx/html/syncmap/index.html
